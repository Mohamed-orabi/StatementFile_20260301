{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // Base URL of StatementFile.Api.
  // In development run the API on port 5100 and Blazor on port 5000.
  // In production set this to the deployed API URL.
  "StatementApiBaseUrl": "http://localhost:5100"
}
